# Backlog

Outstanding repo work lives here when it should not interrupt a timed KodeKloud task.

## Pending

- Migrate existing `Day N/` directories into the future-facing `days/day-XX/` layout when the user wants that cleanup.

## Done

- Pending entries move here after completion, ideally with the commit hash once the user commits.
